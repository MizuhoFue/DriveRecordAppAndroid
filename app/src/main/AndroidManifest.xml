<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.driveandroid">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--新しく追加-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

<!--カメラ用-->
    <!--<uses-feature android:name="android.hardware.camera"
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!- カメラ用 -->
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- カメラ許可 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" /> <!-- 端末カメラ起動 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.driveandroid.MoneyInsertActivity"></activity>
        <activity android:name="com.example.driveandroid.CameraActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDg-tSWdxnQIEO0Zmg-KFITaCHTLdxQDGM" />


        <activity
            android:name="com.example.driveandroid.MapsActivity"
            android:label="@string/title_activity_maps" />


        <activity android:name=".DaoActivity" />
        <activity android:name=".FolderDetail" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FolderList" />
        <activity android:name=".SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.driveandroid.FolderCreate"
            android:label="@string/main" />

           <activity android:name=".Support"/>

        <activity
            android:name="com.example.driveandroid.Convention"
            android:label="@string/main" />


    </application>

</manifest>